# Holding
Provenance: authored — a concern page. It makes no code claims of its own; every measurement it cites is anchored on the canvas or in docs/deep_clean.md.

Read from the code, true when it was read — but not yet placed on a map.

:::elsewhere
**A waiting room, not a destination.** Everything here was traced against a real file and is worth keeping. None of it has earned a block yet, because a block is something a reader can point at. As each journey gets its band, the matching material leaves this page and joins it. **The page is healthy when it is shrinking.**
:::

## Measured, unplaced
| what was measured | where it belongs |
|---|---|
| Core is **72.03% statements, 61.36% branches**. Every leaf is exercised; many branches are not. One file reads 0% and is an instrument blind spot — the pulse worker runs in a worker thread, a separate V8 isolate jest cannot instrument across, proved by sabotaging it and watching three integration tests fail. | a coverage band, if one is ever drawn |
| Nine of thirteen language packs have **no oracle**. Their resolvers are tested; their QUERIES are not — nothing checks that a pack captures the right things, only that it parses. | Band 1's parsing container, once there is something to draw besides an absence |
| Three language packs resolve imports by substring containment, so a longer path can win by accident and the answer is order-dependent — the graph can change because a directory listing changed. | a defect once someone owns it |
| **Left this page on 2026-08-17.** `domain`, `interfaces` and `registry` have had the structural pass now — twenty doors across every layer, each on the gate. Four dead subsystems were found doing it, every one of them a feature that ran and did nothing. | done — see `docs/deep_clean.md` |
| **Left this page on 2026-08-17.** `conducks fallback` COULD NOT RETURN TRUE — three of five indicators required, four reading fields no producer writes. Re-measured across three codebases before the decision: 7,220 functions, zero detections. The owner chose removal, so the detector and every surface reading it are gone. | done — ADR 0151 |
| The vault's HISTORY — pulses, the ledger, a chronoscopic diff between two of them. The canvas draws the present tense only. | a band about time |
