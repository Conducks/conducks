# todo29 — the monorepo work ADR 0069 decided and did not build
Status: todo
- Acceptance: conducks answers correctly on all three repository topologies — one git at the workspace root, nested repositories inside it, and no repository at the root at all — and every service of a declared workspace can be analyzed without the others degrading.

## Context

conducks was pointed at `mentorseed` (five declared services, 1,034 files) on 2026-07-31, the first
time it had ever run on a monorepo it did not develop against. ADRs 0069 and 0070 came out of that
and fixed the two defects it exposed: one repository was ending up with several partial vaults, and
cross-service imports were resolving to phantom symbols by basename.

**This file carries what those records decided and deliberately did not build**, plus what the test
surfaced and nobody has looked at. It exists because ADR 0069's `Open:` paragraph named
`todo29#P3` before this file was written — an invented reference, which the standard forbids and
`docs-lint` does not catch, since it only resolves `- Builds:` and `- Depends:` fields and not prose.
That is itself a finding and is recorded in Phase 4.

## Phase 0 — decide before building: what is a git root when there are several
- Builds: 0069
- [x] DONE — the fixtures this task asked for are what produced the three measurements below, and the split it describes is built. Both topologies were constructed and analyzed (nested `.git`-in-`.git`, and no-root-`.git` with per-service repos), and the two roots are now genuinely separate fields: `gitRootFor()` (`chronicle-interface.ts`) resolves the git root PER FILE while `discoverRoot()` answers the workspace root, so `setProjectDir(effectiveRoot)` no longer forces them equal. Original: ADR 0069 decided that the WORKSPACE root and the GIT root are different questions and stop being one field, then did not implement it, because `chronicle.setProjectDir(effectiveRoot)` makes them the same directory and a single-git repository behaves identically either way — which is exactly how the change would ship broken and unnoticed. Nothing here is testable without fixtures, so the fixtures come first
- [x] MEASURED, and the result is WORSE than the prediction — it is a DISCOVERY failure, not a history one. Built a workspace with `conducks.json` and its own `.git` containing a service with its own `.git`. `discoverFiles()` uses `git ls-files` from the anchor, and the outer repository does not track the inner one, so the inner service is COMPLETELY INVISIBLE: 3 units analyzed where 5 exist, and `app/src/inner.ts` is absent from the vault entirely. Its code is never read, never mind its history. ADR 0069 planned a per-file git root; that alone would not have fixed this, because there is nothing to attribute history TO
- [x] MEASURED, and this topology now WORKS for discovery — the opposite of the nested case. Built a workspace with `conducks.json`, no root `.git`, and two services each with their own. It anchors correctly (impossible before ADR 0069's change) and finds all 5 units across BOTH services, because with no repository at the anchor `discoverFiles()` falls back to a filesystem walk rather than `git ls-files`. The irony is worth recording: the topology 0069 called impossible is the one that works, and the one that looked fine silently loses a service
- [x] THE HISTORY HALF, measured on both fixtures, and the difference between them is the finding. Topology 3 returns `null` — git genuinely could not run, and ADR 0049's rule holds, the caller is told nothing rather than something false. Topology 2 returns **`count=0 authors=0`** for a file that HAS a commit: git ran successfully against the OUTER repository, which honestly knows nothing about that path. A confident zero from a healthy subprocess pointed at the wrong repository is the more dangerous of the two, because no error surfaces anywhere
- [x] So the per-file git root is NOT the whole fix and Phase 1 is rewritten below. Discovery must descend into a nested repository before history has anything to attribute, and `count=0` must become distinguishable from "genuinely never committed"

## Phase 1 — discovery descends into nested repositories, then history follows per file
- Depends: todo29#P0
- [x] DONE. `discoverFiles()` now asks EVERY repository under the anchor, not only the anchor's own — a bounded walk finds each `.git` and runs `git ls-files` with that root as cwd. Nested fixture **3 units -> 5**. Original: FIRST, because nothing else matters until the code is read: `discoverFiles()` must find files inside a nested repository. It runs `git ls-files` from the anchor, and a nested repo is not tracked by its parent, so an entire service disappears. Decide between running `git ls-files` once per discovered repository root, and falling back to the filesystem walk that topology 3 already proves works. Fixed when the nested fixture analyzes 5 units, not 3
- [x] DONE. `gitRootFor()` walks up to the nearest `.git`, cached per directory, and the four per-file history calls run with that as cwd. Nested fixture's inner file **count=0 authors=0 -> count=1 authors=1**, and the workspace-level file still reads from the workspace repository
- [x] VERIFIED, and it caught a regression I introduced. Discovery on conducks returns 524 files against `git ls-files`, the only omissions being 3 PNGs excluded as binaries by design. But the first version of the merge broke the THIRD topology in the opposite direction: returning early only when the anchor is a repository made a mocked unit test fall into the FS path it never used to reach, and a naive merge dropped `conducks.json` — 5 units became 4, and the missing file was the one that DECLARES the workspace. The merge is now narrowed to genuine partial coverage: nested repositories found AND the anchor not being one
- [>] Deferred, blocked on the jest ESM setup rather than on this code: `getDiscoverySurface()` uses a dynamic `import()` the ESM VM cannot resolve, so the FS-merge path is unreachable from a unit test and the sibling chronicle test mocks exec to avoid it. VERIFIED OUT OF BAND on the fixture instead (5 units including the declaration), so the behaviour is checked and only the pin is missing — which is a weaker position than it reads, because nothing stops a later edit breaking it silently. Was `[ ]` with "or a written reason says why it cannot" as an acceptance clause its own text satisfied, so it could never be closed or counted honestly. Fixed when the jest ESM config resolves dynamic imports and the path gets a real test
- [x] RESOLVED BY CONSTRUCTION rather than by a guard: with the git root resolved per file, a command can no longer run against the wrong repository, so the `count=0` case does not arise. Kept as a note because the underlying distinction still has no expression in the type — a zero from a repository that has never heard of a path is not the same fact as a zero from one that has. Original: `count=0` from a git command that ran against the wrong repository must not be reportable as a fact. ADR 0049 drew this line for a subprocess that FAILED; this is a subprocess that SUCCEEDED and answered about the wrong thing, which the existing distinction does not cover

## Phase 2 — the services nobody analyzed
- Builds: 0069
- [x] DONE — answered in full by the task directly below, which is its own completion and was written the same day; this line stayed `[ ]` and the board counted one open task that nothing was owed for. All five services went through a pulse, and the `packages/core` question it singles out is answered concretely: 951 `app` → `packages/core` edges resolve to real nodes. Original: Only `app` and `database` were ever analyzed on mentorseed. `admin`, `packages/core` and `packages/product` have never been through a pulse, so three of five declared services are untested surface. `packages/core` matters most: it is the target of the `@/core` alias that produced 148 of the 163 phantom imports ADR 0070 fixed, so analyzing it is what would prove those edges now RESOLVE rather than merely stop being invented
- [x] DONE AND DEMONSTRATED 2026-07-31. All 974 units of all five services analyzed into ONE workspace vault in 30 s: app 3,010 nodes, admin 1,037, packages/core 1,000, packages/product 189, database 29. **1,626 cross-service edges resolve to real nodes** — app to packages/core 951, app to packages/product 291, admin to packages/core 271, admin to packages/product 91, packages/product to packages/core 22. Every one of those is impossible with per-service vaults, because each end would be in a different database. ADR 0069's central argument is now a measurement rather than an argument

## Phase 3 — a nested declaration, and other things left open
- Builds: 0069
- [x] FIXED AND VERIFIED END TO END (ADR 0071). Re-analyzed all 974 mentorseed units: dangling `IMPORTS` **193 -> 14**, all dangling **701 of 18,673 (3.8%) -> 194 of 19,014 (1.0%)**, now better than conducks scores on itself (1.7%). `ALIASES` edges **0 -> 11** — ADR 0059 classified that edge type as resolvable on reasoning alone and said so; this is the first time any query ever produced one. The named offenders are gone: `server/index.ts::db` carried 103 references and the largest remaining target now carries 3. The fix was two tree-sitter query patterns and nothing else — `@isBinding`, `processAlias` and `RESOLVABLE.ALIASES` were all already built and wired, and unreachable because no query in any language emitted the captures. Original: 180 of 193 dangling `IMPORTS` in the five-service vault carry an `@/core` alias whose FILE resolves correctly. The failure is one level deeper — a BARREL RE-EXPORT. `@/core/database/server` resolves to `packages/core/database/server/index.ts`, which contains `export { coreDb as db, pool, query } from './DatabaseManager'`, so the binding `db` is re-exported under a NEW NAME and defined nowhere in the file the import points at. Verified directly: `ImportProcessor.resolve()` returns the right file for these specifiers, so this is symbol resolution through a re-export chain, not path resolution. Fixed when an import of a re-exported binding lands on the symbol's DEFINING file, and a renamed re-export (`x as y`) resolves to `x`
- [ ] ADR 0069's own open question: whether a service should be able to declare its OWN `conducks.json` and become its own workspace — a vendored dependency that is itself a monorepo is the case. The rule as written takes the NEAREST declaration walking up, so a nested one wins for paths beneath it. That is probably right and is completely untested
- [x] GONE, and the premise was stale rather than the fix missing. RE-MEASURED 2026-08-01 against the five-service vault: **zero** dangling `IMPORTS` carry a bare package specifier. All 14 that remain are project paths (`packages/core/index.ts::browserstoragemanager` and nine more) — the barrel and wildcard residue Phase 3b tracks, a different failure entirely. ADR 0071's re-export work closed this on the way past without the task being restated. TWO REAL BUGS WERE FOUND while confirming it and both are recorded rather than left in the code: `import.ts:87` takes `specifier.split('/')[0]`, so `@playwright/test` is looked up as `@playwright` and a scoped package can never match; and `registerExternalPackage()` has NO production caller, so `context.isExternalPackage()` is always false and step 2 never fires at all. Neither is currently reachable as a dangling edge because induction mints the ecosystem node downstream — carried to Phase 3b rather than closed silently. Original: FROM ADR 0070: 18 dangling `IMPORTS` remain on mentorseed from BARE package specifiers (`next` 13, `@playwright/test` 4, `@vercel/analytics/next` 1) that missed the step-2 external-package check and fell into the same step-4 basename fallback. Same failure family, different entry point, and the record says plainly that no todo carried it — this one does now
- [-] DROPPED as a duplicate, not as work — the classification it asks for is the same classification `todo29#P3b`'s last task asks for, written later and against current numbers (194 of 19,014 after ADR 0071 re-analyzed all five services, where this line still quotes the pre-0071 165 of 10,657). Two tasks for one job means the board counts the debt twice and whichever is closed first makes the other look done. The work is still owed, in one place. Original: 165 dangling edge targets remain of 10,657 edges (1.5%), down from 470 of 10,933 (4.3%). That is now BETTER than conducks scores on itself (1.7%), so it is not obviously a defect — but nobody has looked at what the remaining 165 are, and the 18 above are only part of them
- [ ] 60 nodes carried no edge at all on mentorseed against 19 on conducks, and only 5 `GOVERNS` edges were derived. The `GOVERNS` count is probably honest — `app/docs` holds 1 todo and 0 decisions while the 31 ADRs live in the root tree that was never analyzed — but both numbers are unexplained and were noticed rather than investigated

## Phase 3b — what the barrel fix left open
- Builds: 0071, 0077
- [x] HANDLED — the "cannot" was about the wrong stage. The objection is real at PARSE time, where waves clear the graph, but at `IntraLinker` time the whole graph is loaded, the barrel's whole-file `IMPORTS` edge names the file the wildcard re-exports from, and its symbols are present. The edge is pointed at the real definition rather than a node being minted at the barrel, so nothing is invented. `packages/core/index.ts::browserstoragemanager` now resolves. Original: `export * from './x'` wildcard re-exports are NOT handled and cannot be by this approach
- [>] PARTLY DONE — `collapseAliasChains` now follows ALIASES past one hop, and CYCLES terminate by construction rather than by a depth cap: each step must land on a node not already in `seen`, which only grows, so N distinct alias nodes take at most N steps and a cycle stops at the repeat keeping the last real node. DEFERRED for the remainder, with a reason: the PLAIN leg of a chain is still not followed, because a plain re-export deliberately emits no `ALIASES` edge (ADR 0071), so mentorseed's `server.ts::db -> server.ts::coredb` leg needs hopping through the barrel's imports — which requires telling a re-export node from a definition node, and the graph does not carry that fact (`index.ts::coredb` and `databasemanager.ts::coredb` are indistinguishable on the shallow-loaded properties). Doing it anyway is the guess ADR 0070 refused. Original: A multi-hop `ALIASES` chain is only partially followed
- [x] CLASSIFIED 2026-08-01, all 194 across 130 distinct targets, by querying the vault rather than reading code. `CALLS` 115, `ACCESSES` 54, `IMPORTS` 14, `EXTENDS` 6, `TYPE_REFERENCE` 3, `CONSTRUCTS` 2. Five groups, and only two of them are defects
- [x] TWO ROOT-CAUSE BUGS FIXED (ADR 0077), and the first attempt was a MEASURED REGRESSION worth recording. `registerExternalPackage()` had no production caller, so step 2 of the resolver was dead code that looked live, and `specifier.split('/')[0]` made `@playwright/test` look up as `@playwright`. Manifests are now read BEFORE the wave, from EVERY manifest in the tree (on a monorepo `next` is declared in `app/package.json` and the root declares nothing) and travel into the workers the way global symbols do. The FIRST fix let step 2 answer, and that destroyed the graph: nodes 5,997 -> 3,182, edges 19,014 -> 6,179, because package level is coarser than the `lib::<pkg>::<symbol>` nodes induction already mints. Dangling COUNT fell 194 -> 99 and the RATE went 1.02% -> 1.60% — a count improving while the rate worsens is a denominator being destroyed. Shipped version uses the manifest only to REFUSE the basename fallback: **nodes 6,002, edges 19,008, dangling 194 -> 182, rate 1.02% -> 0.96%**, six wrong edges gone, conducks on itself unchanged and `audit` green
- [x] LARGELY CLOSED by `IntraLinker` step 3e, which binds a bare name to `<package>::<symbol>` only where the unit ALREADY has a resolved edge naming that package and the workspace attests that symbol under it. Built from EDGES, not nodes, because virtual induction mints those nodes AFTER this linker runs — reading nodes would have made the fix work on the second pulse and not the first, which is the kind of bug that looks like flakiness. Refuses when two candidate packages answer. 21 icon `ACCESSES` edges REMAIN, in units with no other resolved edge into the package; that refusal is working as designed and the root-cause fix for them is `reflector.ts:643` (recorded below). Note the brief's "48 edges / 28 targets" was itself wrong — verified as 28 edges / 21 targets. Original: GROUP 1
- [x] ROOT-CAUSED AND FIXED. `unitSymbols` was keyed only by `node.properties.name`, but a member's node id is `<file>::<owner>.<member>` with `name` holding the LAST SEGMENT only (`create`), while the call processor emits the target exactly as written (`userrepository.create`). The qualified form was never a lookup key, so the match could never happen. Adding the qualified key fixed it. Original: GROUP 2 — 33 edges over 22 targets are members of an unresolved receiver
- [-] GROUP 3 — **23 edges over 10 targets are built-in methods on local values** (`industries.map`, `popular_languages.filter`, `branding.name.tolowercase`). DROPPED: these can never be project nodes, because the receiver is local and the METHOD is the JavaScript language. They are correctly unresolvable. They do inflate the dangling count, which is worth knowing when reading the 1.0% figure, but there is nothing to fix
- [-] GROUP 5 — **76 edges over 60 targets are bare symbols with no file and no receiver** (`sendmessageaction`, `onboardingstep`, `checkoutroute`). DROPPED as a group because it is not one: it is the residue left after the four groups above, and splitting it further needs per-symbol work with no shared cause. Group 1's fix will consume the external share of it, and what survives that is the honest remainder to look at next

## Phase 4 — a reference nobody can follow is worse than an admitted gap
- Builds: 0069, 0075
- [x] RESOLVED, and the record stays frozen. The reference ADR 0069 invented now RESOLVES — this file exists and its Phase 3 carries exactly the open question that paragraph handed on, so the citation is correct as written and there is nothing to amend. What was wrong was the ORDER (the number was written before the record), and that is a process failure a record cannot fix by being edited; it is fixed by the task below, which makes the gate refuse the next one at write time
- [x] DECIDED AND BUILT — ADR 0075, the linter resolves them. `proseRefLint()` (`docs-board.ts`) resolves every `todoNN#PN` and `ADR NNNN` written outside a fenced block, against open AND `completed/` todos. MEASURED first: 53 phase references and 265 ADR references live in prose here and all 318 already resolve, so this guards the next one rather than clearing a backlog — which is why it was mutation-checked rather than trusted, and why the three tests that assert a failure were confirmed to go red with the call commented out. It caught ADR 0075 itself while that record was being written. A BARE four-digit number is deliberately NOT resolved: `0.05`, `1,500` and a byte count are the same shape, and a rule that guessed would fail the gate on measurements. `conducks-docs §5.4` was updated in the same change, because a gate rule the code enforces and the standard never mentions is the prose-versus-parser drift todo22#P4 tracks
