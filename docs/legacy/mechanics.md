# Conducks — The Theory of Structural Resonance 🧬

Conducks is not a search engine; it is a **Deterministic Structural Intelligence Platform**. It transforms raw source code into a mathematical graph (the **Synapse**) that reflects the true functional architecture of your system.

---

## 🏛️ 1. Atomic Structural Induction 🏺
The process of "reading" code in Conducks is called **Induction**.

- **Wasm-Powered**: Unlike traditional tools that use fragile regex, Conducks uses **Web-Tree-Sitter** (Wasm) to build a precise Abstract Syntax Tree (AST) for every file.
- **Spectrum Extraction**: As the system crawls your code, it extracts "Spectra"—unique structural DNA including imports, definitions, and call sites.
- **Neural Binding**: The orchestrator binds these disparate spectra into a unified graph, resolving cross-layer dependencies with 100% precision.

---

## 🏎️ 2. Kinetic Physics (The Metrics)
Every node in the Conducks Synapse is assigned physical properties that indicate its structural health and importance.

| Metric | Scientific Definition | Architectural Meaning |
|---|---|---|
| **Gravity** | In-degree centrality (Weighted) | **Pillar Power**: How many other components depend on this symbol. |
| **Risk** | Complexity + Structural Debt | **Blast Radius**: If this node breaks, how much of the system fails? |
| **Entropy** | Historical churn rate | **Instability**: How often is this structural unit changing? |
| **Resonance** | Graph-wide stability score | **Integrity**: How well-composed is the local cluster? |

---

## 🧬 3. The Apostolic 8-Layer Taxonomy
To eliminate ambiguity, Conducks classifies every symbol into a language-agnostic hierarchy.

| Rank | Layer | Description |
|---|---|---|
| **0** | **ECOSYSTEM** | External dependencies (npm, pip, go-mod). |
| **1** | **REPOSITORY**| Individual project or microservice boundary. |
| **2** | **NAMESPACE** | Organizational units (Folders, Packages, Namespaces). |
| **3** | **UNIT** | Structural containers (Files, Modules). |
| **4** | **INFRA** | Cross-cutting concerns (Routers, Controllers, Decorators). |
| **5** | **STRUCTURE** | Foundational definitions (Classes, Interfaces, Structs). |
| **6** | **BEHAVIOR** | Execution logic (Functions, Methods, Constructors). |
| **7** | **ATOM** | Declarative state (Variables, Constants, Fields). |
| **8** | **DATA** | Raw values (Literals, JSON, Parameters). |

---

## ⚖️ 4. The Law of Determinism
Standard coding AI (LLMs) relies on **Heuristics** (guessing). They guess where a function is defined based on its name.
**CONDUCKS relies on Proof.** If two nodes are linked in the Synapse, it is because a mathematical relationship was proven during Induction.

> [!TIP]
> Use `conducks analyze` regularly to ensure your Synapse remains in absolute resonance with your HEAD commit.
