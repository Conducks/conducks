# Testing — Fixture Repo
Provenance: authored — malformed on purpose: F1.T1 is used twice. Used only by
testing-parser.test.ts to prove the parser refuses a repeated task id rather than silently
overwriting one of the two questions it addresses.

## The window
chrome that has to earn its pixels

### F1 — The bar across the top
- How: One row, full window width.
- [ ] F1.T1 The row runs edge to edge.
- [ ] F1.T1 A second, different task wrongly given the same id.
