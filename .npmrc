# tree-sitter@0.25 runtime + grammar packages whose peerOptional ranges predate 0.25
# (e.g. tree-sitter-rust wants ^0.22.1). They are ABI-compatible on the 0.25 runtime;
# allow the install to proceed instead of erroring on peer resolution.
legacy-peer-deps=true
