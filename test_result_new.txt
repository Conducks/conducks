we got this: **REQUIRED FIRST STEP**. Run this to analyze the directory structure and establish rules. Initialize the CONDUCKS folder structure. **Run this ONCE** at the start of a new project.

Arguments

```json
{
  "workspace_path": "application"
}
```

Response

Plain TextRich DisplayMarkdown

PROJECT INITIALIZED Root: application | Mode: heuristic (no .git) Project: application | Subprojects: application CREATED (3 folders) application jobs/to-do jobs/done-to-do ORGANIZATION RULES GROUPING: 2+ criteria match = same file - User Journey (discovery/engagement/contribution/management) - Complexity (low/medium/high) - Team (frontend/backend/data/ml/infra) - Change Frequency (stable/evolutionary/experimental) SPLIT TRIGGERS: 50 tasks OR 20k chars OR 6 months OR 3+ teams DOMAIN TYPES: User Journeys: <entity>-discovery, <entity>-engagement, <entity>-information Data: data-model-core, data-processing, data-storage, data-api Media: media-basic, media-advanced, media-cdn Infrastructure: system-deployment, system-monitoring, system-scalability ML: ml-content-processing, ml-recommendations, ml-search Integration: integration-third-party, integration-internal VERSIONING: v1→v2→v3 (evolution), core+social (offshoot) NEXT: create_job → create_task (per job) → complete_job when all tasks done Jobs now own tasks directly (jobs/job_<id>/tasks/) | Domain files deprecated


it mentions depracated stuff, i want you to search the whole codebase and remove all depracted stuff or mention of depraction as what is the point, no extra useless detail.

when the workspace is getting created, we have to mention this: 
they can also include the tasks so both job and its task is created same time. 

they can also do the single task creation which creates a single task in the job by appending.

and also a batch task creation which creates a group of task 

we need to create tool and connect and add it to documentation and create tests for all 3


and also the folder for the application is not precreated such as to-do done-to-do and the others check docs

in the task they have to mention what folder they want the task md in so we can precreate it using those fields in that folder. 

solve these
